var mongoose = require('mongoose');
var Schema = mongoose.Schema;

const userSchema = new Schema({
    pseudo: String,
    password: String,
    numberOfGamePlayed: Number,
    numberOfGameWon: Number,
    isConnected: Boolean
});

var Users = mongoose.model('users', userSchema);

module.exports = {
    Users: Users
}