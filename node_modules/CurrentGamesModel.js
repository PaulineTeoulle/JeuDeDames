var mongoose = require('mongoose');
var Schema = mongoose.Schema;

const currentGameSchema = new Schema({
    pseudo1: String,
    pseudo2: String,
    matrice: [
        [Number]
    ],
    starter: String
});

var CurrentGames = mongoose.model('partiesencours', currentGameSchema);

module.exports = {
    CurrentGames: CurrentGames
}